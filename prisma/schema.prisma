generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:shiba.db"
}

model Activity {
  id        Int      @id @default(autoincrement())
  name      String
  type      String
}